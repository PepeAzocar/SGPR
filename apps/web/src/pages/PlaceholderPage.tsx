export function PlaceholderPage({ title }: { title: string }) {
  return (
    <div>
      <h1>{title}</h1>
      <p className="hint">Esta sección está creada como acceso de menú. Su funcionalidad se implementará próximamente.</p>
    </div>
  );
}
